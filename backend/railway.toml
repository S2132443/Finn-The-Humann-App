# Railway configuration for Backend Service (FastAPI)

[build]
builder = "DOCKERFILE"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Health check
healthcheckPath = "/docs"
healthcheckTimeout = 100
