# Finn frontend env vars.
#
# Production (served from FastAPI on same origin): leave as /api/v1.
# Dev with Vite proxy (npm run dev): leave as /api/v1 — vite.config.ts proxies /api to :8000.
# Dev without proxy (rare): set to http://localhost:8000/api/v1.
VITE_API_BASE_URL=/api/v1
