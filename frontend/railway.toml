# Railway configuration for Frontend Service (Flask)

[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "gunicorn -w 4 -b 0.0.0.0:$PORT 'app:create_app()'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

# Health check
healthcheckPath = "/"
healthcheckTimeout = 100
